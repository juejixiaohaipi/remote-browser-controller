# BAP Gateway WebSocket URL (include /ws path)
BAP_GATEWAY_URL=ws://192.168.0.100:3000/ws

# Local relay listen port (Chrome extension connects here)
RELAY_PORT=17799

# Device identity (seen by BAP Gateway)
DEVICE_ID=rbc-relay
DEVICE_NAME=RBC-Relay

# Auth token (must match BAP server's DEFAULT_TOKEN)
TOKEN=XERJS7O4y_NF4fzyAlalN3i0udAd6wuT
